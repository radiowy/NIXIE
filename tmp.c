
					//GPIO_WriteReverse(GPIOE, GPIO_PIN_0);//wy³¹czenie katody wyœwietlacza 1
					//GPIO_WriteReverse(GPIOE, GPIO_PIN_1);//wy³¹czenie katody wyœwietlacza 2
					//GPIO_WriteReverse(GPIOE, GPIO_PIN_2);//wy³¹czenie katody wyœwietlacza 3
					//GPIO_WriteReverse(GPIOE, GPIO_PIN_3);//wy³¹czenie katody wyœwietlacza 4
				
				/*
				// Set the brightness from 0 to 999 in a loop (fade up).
        for (brightness = 0; brightness < 1000; ++brightness) 
				{
            // Set the brightness of the LED by modifying the capture compare register
            // for TIM3's channel 2.
            TIM2_SetCompare1(brightness);
            // Delay a short while.
            delay();
        }
				// Set the brightness from 1000 to 1 in a loop (fade down).
        for (brightness = 1000; brightness > 0; --brightness) 
				{
            // Set the brightness of the LED.
            TIM2_SetCompare1(brightness);
            // Delay a short while.
            delay();
        }
				*/